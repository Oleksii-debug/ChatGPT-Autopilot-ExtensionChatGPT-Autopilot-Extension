ChatGPT Автопілот 0.9.14 — AGENT PLATFORM MVP / SECONDS + MILLION CYCLES / SCENARIO HARDENING

- Додано prompt-first Browser Agent: AI є planner/operator, Autopilot — browser/runtime body.
- Додано generic DOM/ARIA observation, validated browser tools, native CDP click fallback, fast burst, follow-up owner instructions і durable lifecycle.
- Додано token/model-call/runtime/action/cost budgets з provider usage accounting.
- Додано Agent schedule policy: ONCE / CONTINUOUS / INTERVAL, start/end, daily active window і restart-safe alarms.
- Додано optional origin permissions, adopted-vs-owned tab provenance, transactional tab cleanup і bounded failure recovery.
- Додано Browser Agent Chromium generic-form E2E.
- Ordinary Session minimum interval тепер підтримує seconds/minutes без зміни старих minute cadences.
- Shared URL/shared prompt підтримує до 1 000 000 logical cycles компактно; distinct physical task definitions лишаються bounded.
- Scenario Work отримав write-ahead cleanup obligations, owner epoch, restart-safe identity, dependency hardening та legacy timeout/replacement fixes.
- Orchestration V2 managed tab retirement більше не забуває ownership після tabs.remove failure.
- User-selected timing values не перезаписуються релізом.
- Додано mixed-runtime reliability gate: Ordinary + Browser Agent + Scenario Work + Orchestration V2 одночасно, fault injection, cold restart і фінальний quiescence cleanup.
- Mixed-load виявив і виправив Orchestration coordinator rebind: terminal operation попереднього turn очищається перед переходом Task fresh-root -> concrete conversation URL, schema validation не послаблювалась.
- Stop останньої Ordinary Session тепер гарантує durable retirement retry: retirePending входить у canonical core alarm, має exponential backoff і не блокується provider rate-limit; після quiescence owned-tab backlog = 0.
