ChatGPT Автопілот 0.6.0 — SAFE REMOTE OPENAI-COMPATIBLE ENDPOINTS

НОВЕ
- Універсальний Windows script «НАЛАШТУВАТИ OPENAI-COMPATIBLE АДРЕСУ.ps1» для LM Studio та remote OpenAI-compatible API.
- Старий LM Studio address script збережено як backward-compatible wrapper.
- Remote OpenAI-compatible endpoint дозволений лише через HTTPS.
- Plain HTTP сумісний тільки з localhost / 127.0.0.1 / ::1 для локальних серверів.

БЕЗПЕКА
- Gateway сам валідовує COMPATIBLE_BASE_URL, тому ручна правка gateway-settings.json не обходить transport guard.
- URL з username/password, query string або fragment відхиляється.
- DPAPI credential layer 0.5.1 збережено: compatible key не пишеться в Chrome/JSON/log/package.

БАЗА ЗБЕРЕЖЕНА
- AI Manager + opt-in TUNE_SESSION;
- primary/strong/hybrid routing;
- central bounded FIFO inference;
- web reports / handoff / recovery;
- OpenAI + OpenAI-compatible DPAPI unattended credentials.

QA HARNESS
- Виправлено false-green npm aliases: test:ui/test:release тепер дивляться у фактичні tests-regression paths.
- Додано явні test:ai / test:legacy-ai / test:core / test:interaction / test:reliability.
- npm test тепер послідовно запускає всю матрицю; reliability використовує Node --test-force-exit після TAP completion, щоб відкриті timer handles не тримали runner нескінченно.
