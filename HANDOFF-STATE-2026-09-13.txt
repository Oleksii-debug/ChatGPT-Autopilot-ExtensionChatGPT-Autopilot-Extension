SUPERSEDED DEVELOPMENT SNAPSHOT — 0.9.10 FINAL HARDENING CONTINUES AFTER THIS HANDOFF
The original handoff below described target 0.9.9. A later adversarial pass found additional Ordinary ambiguous-Send, same-conversation ownership, frozen-auditor-snapshot and bounded-replacement issues; final release lineage is now 0.9.10. Historical text below is retained as evidence.

CHATGPT AUTOPILOT EXTENSION — WIP HANDOFF — 2026-09-13

STATUS
- This is NOT a release build.
- package.json/manifest still say 0.9.8.
- Development target for the next qualified release: 0.9.9.
- This snapshot contains work performed AFTER the packaged 0.9.8 release candidate.

LAST TARGETED VERIFICATION
- 65/65 PASS on 2026-09-13 after latest semantic/pipeline/fairness changes.
- Full suite and Chromium qualification have NOT yet been rerun after these latest changes.

LATEST ARCHITECTURAL WORK
- strict AUTOPILOT_SCENARIO_RESULT worker-result contract
- strict AUTOPILOT_AUDITOR_ALLOCATION contract
- dependency DAG / blocked downstream non-materialization
- durable auditor lease / duplicate-auditor prevention
- WAIT_ALL_TERMINAL and TIMEBOXED_AUDIT policies
- FIRST audit launch snapshot validation
- correction in same conversation
- fresh-chat worker replacement after correction exhaustion
- unfinished FIRST correction may continue after TIMEBOXED_AUDIT enters SECOND
- restart persistence for auditor lease and dependency waits
- mixed ordinary/managed scheduler fairness
- ten-session parallelism, independent owned tabs, fixed non-extending profile rate-limit deadline
- bounded ordinary progress under a much larger managed queue

MANDATORY NEXT STEPS
1. Continue critical review of correction/timeout/replacement and evidence semantics; do not assume green targeted tests prove full correctness.
2. Add/finish tests for long-running mixed load: ordinary + Scenario Work + old orchestration + rate-limit + ambiguous Send + tab failures.
3. Run full npm test matrix after latest changes.
4. Run Chromium/browser gates after full matrix.
5. Only then update version metadata to 0.9.9, CHANGES/QA, package twice, compare deterministic bytes/SHA-256, and upload final release artifacts.
6. Do not claim HUMAN_TESTED/NVDA_VERIFIED/OWNER_WINDOWS_CHROME_VERIFIED until physical user testing.

LEGACY FINDINGS THAT MUST INFORM 0.9.9
- 0.1.15.3 has inefficient/conflicting profile-wide scheduling behavior under ordinary+scenario mixed load.
- Old scenario engine used launch/time progression where semantic completion/evidence barriers were required.
- Drive auditor output can be structurally useful, but scheduler must execute dependency semantics instead of treating reservations as immediately runnable.
- Scripture Archive showed duplicate allocation/auditor race requiring durable single-auditor authority.
- WordDeck showed workers launched only to discover DEPENDENCY_NOT_TERMINAL/NOT_SATISFIED.
- Lots of opened tabs without useful work are therefore both transport and semantic-scheduler problems.
