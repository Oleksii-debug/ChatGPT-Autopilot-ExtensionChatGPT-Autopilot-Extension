name: Interaction deterministic tests

on:
  push:
    branches: ["main", "source/0.9.19-canonical-convergence"]
    paths:
      - "src/interaction/**"
      - "tests-regression/interaction/**"
      - "package.json"
      - ".github/workflows/interaction-tests.yml"
  pull_request:
    paths:
      - "src/interaction/**"
      - "tests-regression/interaction/**"
      - "package.json"
      - ".github/workflows/interaction-tests.yml"

permissions:
  contents: read

jobs:
  interaction-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - run: npm run test:interaction
