QA 0.7.0 — PAIRED GATEWAY SECURITY

FINAL AUTOMATED MATRIX:
- New AI / Gateway / Windows companion: 97/97 PASS.
- Core + integration regression: 233/233 PASS.
- ChatGPT interaction regression: 111/111 PASS.
- UI regression: 45/45 PASS.
- Reliability regression: 22/22 PASS (six isolated Node-process runs).
- Legacy AI Manager regression: 18/18 PASS.
- Release gates: 7/7 PASS.
- Разом автоматизованих тестів: 533/533 PASS.

NEW 0.7.0 PROOF:
- normal web origins remain blocked;
- unpaired Chrome extension receives 428 GATEWAY_PAIRING_REQUIRED;
- pairing requires an explicit locally-created 5-minute window;
- OPTIONS/preflight never creates the persisted pairing;
- first real request from a valid 32-character Chrome extension ID binds exactly one extension origin;
- pairing window is consumed after successful binding;
- a different extension origin is rejected with 403 GATEWAY_EXTENSION_NOT_PAIRED;
- expired pairing window cannot bind;
- corrupt persisted pairing fails closed and requires explicit local reset;
- local CLI/no-Origin diagnostics remain available;
- /health and /status expose only safe pairing booleans/expiry metadata, not the paired extension ID;
- Windows companion includes explicit open-pairing and reset-pairing scripts; installer/menu/README point to the secure flow;
- config survives Gateway update because installer preserves config/runtime/logs.

ADDITIONAL GATES:
- JS/MJS/CJS syntax: 141/141 PASS.
- JSON parse: 5/5 PASS.
- deterministic extension packaging: PASS (two byte-identical builds after final QA content).
- ZIP integrity/SHA256: PASS for FINAL / Gateway / SOURCE / UKR COMPLETE.

NOT PHYSICALLY CLAIMED:
- actual PowerShell pairing/reset execution on owner PC;
- physical Chrome extension pairing on owner PC;
- actual remote API/Ollama/LM Studio acceptance;
- overnight multi-profile Chrome run;
- physical NVDA acceptance.
