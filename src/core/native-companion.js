export const NATIVE_COMPANION_HOST_NAME = 'org.chatgpt_autopilot.companion';
export const NATIVE_COMPANION_PROTOCOL_VERSION = 1;

export const NativeCompanionRequestType = Object.freeze({
  HELLO: 'hello', HEALTH: 'health', CAPABILITIES: 'capabilities', FILESYSTEM_READ_TEXT: 'filesystem.readText',
  CREDENTIALS_LIST: 'credentials.list', CREDENTIALS_RESOLVE: 'credentials.resolve', MCP_REQUEST: 'mcp.request', MCP_CLOSE: 'mcp.close',
});
const REQUEST_TYPES = new Set(Object.values(NativeCompanionRequestType));
const ID = /^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$/u;
const RESPONSE_KEYS = new Set(['protocolVersion', 'requestId', 'type', 'ok', 'result', 'error']);
const ERROR_KEYS = new Set(['code', 'message']);
function clean(value,max=4096){return typeof value==='string'?value.trim().slice(0,max):'';}
function exactKeys(value,allowed,label){if(!value||typeof value!=='object'||Array.isArray(value))throw new Error(`${label} must be an object`);for(const key of Object.keys(value))if(!allowed.has(key))throw new Error(`${label} contains unknown field: ${key}`);}
function requestId(value){const id=clean(value,128);if(!ID.test(id))throw new Error('Native Companion requestId is invalid');return id;}
function boundedPayload(value,{maxBytes=256_000,label='payload'}={}){if(value==null)return{};if(!value||typeof value!=='object'||Array.isArray(value))throw new Error(`Native Companion ${label} must be an object`);const out=structuredClone(value);if(new TextEncoder().encode(JSON.stringify(out)).byteLength>maxBytes)throw new Error(`Native Companion ${label} is too large`);return out;}
export function createNativeCompanionRequest(type,payload={},{id=''}={}){const normalizedType=clean(type,80);if(!REQUEST_TYPES.has(normalizedType))throw new Error(`Unsupported Native Companion request type: ${normalizedType||'(empty)'}`);const generated=id||(globalThis.crypto?.randomUUID?.()??`native-${Date.now()}-${Math.random().toString(36).slice(2)}`);return Object.freeze({protocolVersion:NATIVE_COMPANION_PROTOCOL_VERSION,requestId:requestId(generated),type:normalizedType,payload:boundedPayload(payload)});}
export function normalizeNativeCompanionResponse(input,expected={}){exactKeys(input,RESPONSE_KEYS,'Native Companion response');if(Number(input.protocolVersion)!==NATIVE_COMPANION_PROTOCOL_VERSION)throw new Error('Native Companion protocol version mismatch');const normalizedId=requestId(input.requestId);if(expected.requestId&&normalizedId!==expected.requestId)throw new Error('Native Companion response requestId mismatch');const type=clean(input.type,80);if(!REQUEST_TYPES.has(type))throw new Error('Native Companion response type is invalid');if(expected.type&&type!==expected.type)throw new Error('Native Companion response type mismatch');if(typeof input.ok!=='boolean')throw new Error('Native Companion response ok must be boolean');if(input.ok){if(input.error!=null)throw new Error('Successful Native Companion response cannot contain error');const result=input.result==null?{}:boundedPayload(input.result,{maxBytes:900_000,label:'result'});return Object.freeze({protocolVersion:1,requestId:normalizedId,type,ok:true,result,error:null});}exactKeys(input.error,ERROR_KEYS,'Native Companion error');const code=clean(input.error.code,120),message=clean(input.error.message,4000);if(!code||!message)throw new Error('Native Companion error is incomplete');if(input.result!=null)throw new Error('Failed Native Companion response cannot contain result');return Object.freeze({protocolVersion:1,requestId:normalizedId,type,ok:false,result:null,error:Object.freeze({code,message})});}
export class NativeCompanionError extends Error{constructor(code,message){super(message);this.name='NativeCompanionError';this.code=code;}}
export class NativeCompanionClient{
 constructor({chromeApi=globalThis.chrome,hostName=NATIVE_COMPANION_HOST_NAME,createId=null}={}){if(!chromeApi?.runtime?.sendNativeMessage)throw new Error('Chrome nativeMessaging capability is unavailable');this.chrome=chromeApi;this.hostName=clean(hostName,200);this.createId=typeof createId==='function'?createId:null;if(!this.hostName)throw new Error('Native Companion host name is required');}
 async send(type,payload={}){const id=this.createId?this.createId():'';const request=createNativeCompanionRequest(type,payload,{id});let raw;try{raw=await this.chrome.runtime.sendNativeMessage(this.hostName,request);}catch(error){throw new NativeCompanionError('NATIVE_TRANSPORT_ERROR',clean(error?.message||error,4000)||'Native Companion transport failed');}const response=normalizeNativeCompanionResponse(raw,{requestId:request.requestId,type:request.type});if(!response.ok)throw new NativeCompanionError(response.error.code,response.error.message);return response.result;}
 hello(clientVersion=''){return this.send(NativeCompanionRequestType.HELLO,{clientVersion:clean(clientVersion,120)});} health(){return this.send(NativeCompanionRequestType.HEALTH);} capabilities(){return this.send(NativeCompanionRequestType.CAPABILITIES);}
 readText({rootId,relativePath,maxBytes=262144}={}){return this.send(NativeCompanionRequestType.FILESYSTEM_READ_TEXT,{rootId:clean(rootId,128),relativePath:typeof relativePath==='string'?relativePath:'',maxBytes});}
 listCredentials({targetOrigin}={}){return this.send(NativeCompanionRequestType.CREDENTIALS_LIST,{targetOrigin:clean(targetOrigin,2048)});} resolveCredential({credentialId,targetOrigin}={}){return this.send(NativeCompanionRequestType.CREDENTIALS_RESOLVE,{credentialId:clean(credentialId,128),targetOrigin:clean(targetOrigin,2048)});}
 mcpRequest(payload){return this.send(NativeCompanionRequestType.MCP_REQUEST,payload);} mcpClose(commandId){return this.send(NativeCompanionRequestType.MCP_CLOSE,{commandId});}
}
export function createNativeMcpTransportFactory(nativeClient){return async(server)=>({request:async(method,params,meta={})=>nativeClient.mcpRequest({commandId:server.commandId,method,params,timeoutMs:meta.timeoutMs??server.timeoutMs,invocationId:meta.invocationId??''}),close:async()=>{await nativeClient.mcpClose(server.commandId);}});}
